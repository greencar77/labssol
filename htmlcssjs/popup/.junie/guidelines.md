Create simple demo of static HTML page (including CSS and JS) where several 'li' items are shown in index page and when the user click on any of them another html layer pops with some href elements.

When popup appears, the initial screen becomes unavailable but seen.

Popup can be easily removed by clicking outside it or by some keypress like Esc.

Put CSS and JS in separate files.

Avoid third party libraries, use plain HTML, CSS, JS.