Create sample SpringBoot application which demonstrates basic JSP.

Demonstrate:
* list of values display
* new data input (some form with ~3 attributes. data submission happens after the button is pressed)
* most popular jsp tags in action

# Frontend
- JSP
- Save CSS in separate css files
- if there is JavasScript, keep it in own JS files

# Backend
- SpringBoot 3.5.* (latest)