Generate SpringBoot JSP sample application which connects to mysql demo database sakila and outputs some data.

Some 2 pages. list some data from tables.

Use the last SpringBoot 3.5.* version.

Use Maven.