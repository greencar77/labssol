Create project demonstrating backend written in Java.

Backend:
* SpringBoot 4 (latest). Check https://spring.io/projects/spring-boot#support to see that 4.x is already supported.

Stop, if can't guarantee SpringBoot 4.x!

Use Gradle. DO NOT use Maven!
Use gradle which is in 'c:\Gradle\gradle-9.6.1'
Add some unit tests.

Use H2 database with a couple of tables.
Use H2 in file so that it can be opened and observed after the application ends.

Use JSP for frontend.

Have 2-3 endpoints (for data display and data addition)

Take Maven from 'C:\Program Files\JetBrains\IntelliJ IDEA 2026.1.2' if the command 'mvn' doesn't work directly

At the end of project setup, tell how many tokens were spent during the process.