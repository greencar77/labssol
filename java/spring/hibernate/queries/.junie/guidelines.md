Create sample application which demonstrates different Hibernate query types for sakila db (mysql).

Make it Spring app (not SpringBoot).

make db calls from unit tests.
Demonstrate all main types:
* HQL
* JQL
* Criteria
* native queries
* and other if such exists